#include <string>
#include <iostream>
#include <vector>
#include <stdio.h>
#include <algorithm>
#include <math.h>
#include <queue>
using namespace std;

int main()
{
	int n, x;
	scanf("%d %d", &n, &x);
	for (int i = 0; i < n; i++)
	{
		int tmp;
		scanf("%d", &tmp);
		if (x > tmp && i != n - 1)
			printf("%d ", tmp);
		else if (x > tmp && i == n - 1)
			printf("%d\n", tmp);
	}
	return 0;
}
