#include <iostream>
#include <algorithm>
#include <stdlib.h>
#include <vector>

using namespace std;

int A[1001];

int main()
{
	int sum;
	cin >> sum;
	int A[9];
	for (int i = 0; i < 9; i++)
	{
		cin >> A[i];
		sum -= A[i];
	}
	cout << sum;
	return 0;
}
