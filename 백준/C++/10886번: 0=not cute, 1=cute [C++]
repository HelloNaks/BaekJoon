#include <iostream>
#include <algorithm>
#include <stdlib.h>

using namespace std;

int main()
{
	int n, sum = 0;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		int a;
		cin >> a;
		if (a == 1)
			sum++;
		else
			sum--;
	}
	if (sum > 0)
		cout << "Junhee is cute!";
	else
		cout << "Junhee is not cute!";
	return 0;
}
